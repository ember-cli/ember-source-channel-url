language: node_js
node_js:
  - "6"
  - "8"

cache:
  yarn: true

branches:
  only:
    - master

before_script:
  # ensure we are testing against latest versions of all deps when using Yarn
  - yarn upgrade
